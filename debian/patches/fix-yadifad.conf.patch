From: Markus Schade <markus.schade@gmail.com>
Date: Fri, 05 Jun 2015 10:40:00 +0200
Subject: fix spelling, paths and lintian whatis warning

--- a/doc/yadifad.conf.man
+++ b/doc/yadifad.conf.man
@@ -14,13 +14,12 @@
 .SH NAME
 
 .PP
-yadifad.conf
-is the configuration file for \fIyadifad\fP(8)\&.
+yadifad.conf \- is the configuration file for \fIyadifad\fP(8)\&.
 .PP
 .SH SYNOPSIS
 
 .PP
-${PREFIX}/etc/yadifad.conf 
+/etc/yadifad.conf 
 .PP
 .SH DESCRIPTION
 
@@ -48,14 +47,14 @@ Access lists.
 .PP
 The configuration supports included files.
 .br
-example: include /usr/local/var/zones/master/yadifad\-2.conf 
+example: include /etc/yadifa/conf.d/local.conf 
 .PP
 The configuration files can be nested. 
 .PP
 The configuration consists of: 
 .TP
 .B *
-Container, which starts with <contai/er name> and ends with </container name> 
+Container, which starts with <container name> and ends with </container name> 
 .TP
 .B *
 Variable name 
@@ -112,21 +111,22 @@ Main without includes
         chroot                  off
 
         # The path of the log files
-        logpath                 "/usr/local/var/log"
+        logpath                 "/var/log/yadifa"
 
         # The location of the pid file
-        pidfile                 "/usr/local/var/run/yadifad.pid"
+        pidfile                 "/var/run/yadifa/yadifad.pid"
 
         # The path of the zone files
-        datapath                "/usr/local/var/zones"
+        datapath                "/var/lib/yadifa"
 
         # The path of the DNSSEC keys
-        keyspath                "/usr/local/var/zones/keys"
+        keyspath                "/var/lib/yadifa/keys"
 
         # The path of the transfer and journaling files (AXFR & IXFR)
-        xfrpath                 "/usr/local/var/zones/xfr"
+        xfrpath                 "/var/lib/yadifa/xfr"
 
-        # A string returned by a query of hostname. CH TXT (note if you leave this out, the real hostname will be given back)
+        # A string returned by a query of hostname. CH TXT (note if you leave
+        # this out, the real hostname will be given back)
         hostname                "server\-yadifad"
 
         # An ID returned by a query to id.server. CH TXT
@@ -138,7 +138,8 @@ Main without includes
         # Enable EDNS0 support (?)
         edns0                   on
 
-        # Set the maximum UDP packet size.  Packetsize annot be less than 512.  or more than 65535.  A typical choice is 4096.
+        # Set the maximum UDP packet size. Cannot be less than 512. Cannot be
+        # more than 65535.  Typical choice is 4096.
         edns0\-max\-size          4096
 
         # The maximum number of parallel TCP queries.
@@ -198,6 +199,10 @@ Main without includes
         # allow\-notify any
         allow\-notify            master,admins
 
+        # If YADIFA has the controller enabled, allow control only for these
+        # clients (none by default)
+        allow\-control               localhost
+
 </main>
 .Ve
 .RE
@@ -260,6 +265,7 @@ Master\-slave key use
     transferer  key master\-slave
     admins      192.0.2.0/24, 2001:db8::74
     master      192.0.2.53
+    localhost   127.0.0.0/8, ::1
 </acl>
 .Ve
 .PP
